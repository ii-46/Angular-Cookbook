import { ScollToDirective } from './scoll-to.directive';

describe('ScollToDirective', () => {
  it('should create an instance', () => {
    const directive = new ScollToDirective();
    expect(directive).toBeTruthy();
  });
});
